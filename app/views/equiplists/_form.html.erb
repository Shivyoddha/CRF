<%= form_with(model: equiplist) do |form| %>
  <% if equiplist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equiplist.errors.count, "error") %> prohibited this equiplist from being saved:</h2>

      <ul>
        <% equiplist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :calibrate %>
    <%= form.date_select :calibrate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
