<p id="notice"><%= notice %></p>

<h1>Training2s</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @training2s.each do |training2| %>
      <tr>
        <td><%= link_to 'Show', training2 %></td>
        <td><%= link_to 'Edit', edit_training2_path(training2) %></td>
        <td><%= link_to 'Destroy', training2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Training2', new_training2_path %>
