<%= form_with(model: training_list) do |form| %>
  <% if training_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training_list.errors.count, "error") %> prohibited this training_list from being saved:</h2>

      <ul>
        <% training_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
